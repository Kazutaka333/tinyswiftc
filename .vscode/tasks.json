{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cmake then build",
      "type": "shell",
      "command": "rm -rf build && cmake -S . -B build && cmake --build build",
    }
  ]
}